import React from 'react';
import {SafeAreaView, ScrollView, Text} from 'react-native';

const Package = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Package</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Package;
